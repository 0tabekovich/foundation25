class bir:
    def __init__(self ,x,ls):
        ls=list()
        self.ls=ls
        self.x=x
        if type(x)==int:
            ls.append(x)
    def show(self,ls):
        print(self.ls)
class ikki(bir):
    def __init__(self):(self):
        super().__init__(self.ls,self.x)
        if type(self.x)==int or type(self.x)==float:
            self.ls.append(self.x)
class uch(ikki):
    def __init__(self):(self):
        super().__init__(self.ls,self.x):
        if type(self.x)==int or type(self.x)==float or type(self.x)==bool:
            ls.append(x)
class tort(uch):
    def __init__(self):
        super().__init__(x);
        if type(x)==int or type(x)==float or type(str) or type(x)=bool:
            ls.append(x)
son=bir.__init__(self=bir,)
som